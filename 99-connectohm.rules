# if we get a USB serial device node, create a ctohm symlink and start the ctohm-settings service
# NOTE: we have to do it as a systemd service to escape udev sandboxing and prevent blocking
KERNEL=="ttyUSB[0-9]", SYMLINK+="ctohm%n", TAG+="systemd", ENV{SYSTEMD_WANTS}="ctohm-settings.service"
