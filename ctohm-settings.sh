#!/bin/bash

# load config values, which should is formatted so we can just include it directly
source /etc/connectohm.conf

# Translate those numbers to parameters for stty

# Set tty params

# Kill the previous program mode

# Run the new mode
